const config = {
    host: "https://www.easy-mock.com/mock",
    project: "/5aab842b82fe290e7f22d91a"
}
// const url = config.host + config.port;
const url = config.host + config.project;
export default {
    url: url,
    mockCtrl: false
};